# AI Teammate Frontend Milestones

The current milestone is defined in `milestones.json`.

M7 preserves the completed backend-residue removal and validates this authoritative AI Teammate
package boundary end to end against the single shared backend. The non-secret contract pin is source;
CLI-generated configuration, manifests, and packages are protected local operational state. Approved
Agent 365 package and tenant operations use CLI-owned workflows with a reviewed dry run, rollback
boundary, and explicit approval. The current backend is revision `0000028`, and a sanitized
same-revision replay remains open. The protected OBO-side `.a365/ai-teammate` snapshot is historical
output only and must never be used for publication.
